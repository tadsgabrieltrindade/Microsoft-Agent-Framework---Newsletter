# Camada de Infraestrutura

> Essa camada é responsável por conter as implementações de acesso a dados, serviços externos e outras dependências da aplicação. Ela é dependente da camada Core, mas não deve conter nenhuma lógica de negócio, apenas a implementação das interfaces definidas na camada Core.